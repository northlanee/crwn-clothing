import React, { FC, ReactElement } from "react";

import { SignIn } from "../../components";

const Auth: FC = (): ReactElement => {
  return (
    <div>
      <SignIn />
    </div>
  );
};

export default Auth;
